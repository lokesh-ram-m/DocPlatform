---
sidebar_position: 4
---

# Data, Storage & Messaging

## Databases
The application uses **SQL Server** as its database. This is confirmed by the database capability detected in the application's metadata.

## Data Access Approach
The data access approach utilized in the application is **Dapper**, which is a lightweight micro-ORM (Object-Relational Mapper). This technology allows for efficient data access and manipulation without the overhead typically associated with more complex ORM frameworks.

## Entities / Domain Model
The application includes the following entities in its domain model:
- **Project**
- **Task**
- **User**

These entities represent the core data structures managed within the application.

## Caching
No caching mechanism was detected in the metadata for the application.

## Messaging & Queuing
No message queue or event bus was detected in the current metadata.
